#pragma once

namespace pkodev
{
	namespace address
	{
        // Game.exe 0 (1222073761)
        namespace GAME_13X_0
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050AF50;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C2508;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CBE70;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD2E0;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A288;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E390;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x00494040;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B1A0;
        }

        // Game.exe 1 (1243412597)
        namespace GAME_13X_1
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050B060;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C25F8;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CBF80;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD3F0;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A288;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E4A0;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x00494150;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B1A0;
        }

        // Game.exe 2 (1252912474)
        namespace GAME_13X_2
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050B130;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C25F8;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CC020;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD4C0;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A288;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E540;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x004941F0;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B1A0;
        }

        // Game.exe 3 (1244511158)
        namespace GAME_13X_3
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050B120;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C25F8;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CC010;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD4B0;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A288;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E530;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x004941E0;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B1A0;
        }

        // Game.exe 4 (1585009030)
        namespace GAME_13X_4
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050B340;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C2610;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CC220;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD690;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A2A0;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E740;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x004943F0;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B2D0;
        }

        // Game.exe 5 (1207214236)
        namespace GAME_13X_5
        {
            // BOOL	SC_CHARSTR(LPRPACKET pk)
            const unsigned int SC_CHARSTR = 0x0050ADC0;

            // NetIF *g_NetIF
            const unsigned int g_NetIF = 0x006C2510;

            // bool CWorldScene::_Init()
            const unsigned int CWorldScene___Init = 0x004CBCA0;

            // void CWorldScene::_FrameMove( DWORD dwTimeParam )
            const unsigned int CWorldScene___FrameMove = 0x004CD110;

            // CUIInterface
            const unsigned int CUIInterface = 0x0066A290;

            // CForm* CUIInterface::_FindForm(const char * frmName)
            const unsigned int CUIInterface___FindForm = 0x0049E190;

            // CCompent* CForm::Find( const char* str )
            const unsigned int CForm__Find = 0x00493E40;

            // void CLabel::SetCaption( const char * str )
            const unsigned int CLabel__SetCaption = 0x0042B190;
        }
	}
}